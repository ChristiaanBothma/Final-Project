
library(rmsfuns)

packagestoload <- 
c("tidyr", "dplyr" ,"readr", "tibble", "Dplyr2Xts","lubridate", 
  "ggplot2", "e1071", "RandomForest")

# This loads all packages and installs the package(from CRAN only) if not already in library.
load_pkg(packagestoload)

#maik branch

#salut
#test